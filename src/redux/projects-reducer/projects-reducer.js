import Projects from "../../utils/projects-data";

const INITIALE_STATE = {
    projects: Projects,
}

const projectsReducer = (state = INITIALE_STATE, action) => {
    return state;
}

export default projectsReducer;